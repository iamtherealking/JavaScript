'use strict'

window.addEventListener('load', function(e)
{
    const formElements = Array.from(document.querySelectorAll('input')); // "how many things are inside that thing?"

    // for each loop to input console
    
    /*formElements.forEach(function(value,index){
        value.setAttribute("data-index", index);
        value.addEventListener("click", function(e)
        {
            console.log(value);  
        })
    })*/

    // map Array.from()
    
    // read through stuff, check moodle for more stuff
})